version: "3.9"
services:
  paywall-api:
    build:
      context: ..
      dockerfile: paywall-api/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PROXY_SERVER=http://192.168.2.222:1190
    volumes:
      - ../:/app/plugin
    restart: unless-stopped
    shm_size: "2gb"
